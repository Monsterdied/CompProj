import io;
import Quicksort;
import A;

class CompileBasic extends Quicksort {

    public static void main(String[] args) {
        int mainLocal;
        A b;
        A a;
        //b.some(mainLocal);
        //io.quicksort(io.quick(1,2),3);
        //mainLocal = 1;
        //io.quick(1+2,2+3);
        mainLocal = io.quick(io.q(1));

        //mainLocal = 1 + io.quick(1,2);
    }
}