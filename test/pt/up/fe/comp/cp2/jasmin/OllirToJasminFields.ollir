import okidokig.ilikeit.io;

Test {

    .field public intField.i32;

    .field public likeit.io;

    .method public static main(args.array.String).V {

        ret.V;

    }

    .method public foo().i32 {

        putfield(this.Test, intField.i32, 10.i32).V;

        a.i32 :=.i32 getfield(this.Test, intField.i32).i32;

        invokestatic(io, "eat").V;

        ret.i32 a.i32;

    }

    .construct Test().V {
        invokespecial(this, "<init>").V;
    }
}
